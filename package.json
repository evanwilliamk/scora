{
  "name": "scora-monorepo",
  "version": "0.1.0",
  "private": true,

  "scripts": {
    "build": "cd apps/api && npm run build",
    "start": "cd apps/api && npm start"
  },
  "devDependencies": {}
}
